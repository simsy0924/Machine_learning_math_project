# Machine Learning Math Project

수학 동아리 개인 심화 탐구 발표를 위한 **블록형 수학 실험 도구**입니다.

핵심 원칙은 하나입니다.

> 발표 시작 시점에는 신경망이 완성되어 있지 않는다. 발표 중 수학 블록을 연결하면서 신경망을 직접 만든다.

## 현재 구현

- 드래그 가능한 블록 작업공간
- 출력 포트 → 입력 포트 연결
- 일반 수학 블록
  - 숫자
  - 더하기 / 곱하기
  - 제곱 / 합 / 평균
  - ReLU
- 그림 입력
- 그림을 28×28 값으로 변환
- 28×28 값을 784차원 벡터로 펼치기
- 블록 그래프 실제 계산
- 선택한 블록의 수학식과 현재 값 표시

## 의도적으로 없는 것

현재 코드에는 다음을 **미리 구현하지 않습니다.**

- `NeuralNetwork` 클래스
- 완성된 신경망 레이어
- 가중치/편향 전용 블록
- 학습 버튼
- 역전파 구현
- 경사하강법 구현

이 기능들은 발표 내용이 정해지는 과정에 맞춰, 일반 수학 블록에서 어떻게 만들어지는지를 보여주는 방향으로 추가합니다.

## 실행

정적 HTML/CSS/JS 프로젝트라 `index.html`을 직접 열 수 있습니다.

로컬 서버를 쓰려면:

```bash
python -m http.server 8000
```

그 뒤 `http://localhost:8000`으로 접속합니다.

## 다음 후보 작업

1. 행렬·벡터 블록 추가
2. 함수 정의/합성 표현 방식 결정
3. 발표 중 미분을 어떤 수준까지 블록으로 구현할지 결정
4. Quick, Draw! 데이터 로더 연결
5. 최종 단계에서 발표 중 만들어진 함수로 실제 낙서 분류
