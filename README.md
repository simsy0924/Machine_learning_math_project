# Machine Learning Math Project

수학 동아리 개인 심화 탐구 발표를 위한 **블록형 수학 실험 도구**입니다.

핵심 원칙은 하나입니다.

> 발표 시작 시점에는 완성된 신경망이 없다. 일반 수학 블록과 데이터 블록을 연결하면서 필요한 구조를 직접 만든다.

## 현재 구현

### 블록 작업공간

- 터치/마우스로 블록 이동
- 출력 포트 → 입력 포트 연결
- 선택한 블록의 수학식과 현재 값 표시
- 여러 블록을 선택해 하나의 **사용자 블록**으로 묶기
- 사용자 블록은 브라우저 `localStorage`에 저장되어 같은 기기에서 다시 사용할 수 있음

### 기본 블록

- 입력
  - 숫자
  - 변수: 숫자 또는 벡터
  - 그림 입력
- 데이터
  - 불러온 데이터
  - 종류 고르기
  - 샘플 고르기
  - 그림값
  - 종류 번호
  - 종류 벡터
- 생성
  - 같은 값 벡터
  - 무작위 벡터
- 변환
  - 28×28 변환
  - 펼치기
- 연산
  - 더하기 / 빼기 / 곱하기 / 나누기
  - 제곱 / 절댓값 / 최댓값
  - 합 / 평균 / 내적
  - 지수 / 로그
- 미분
  - 변수 이름을 지정해 연결된 스칼라 식을 미분
  - 숫자 변수와 벡터 변수 모두 지원
  - 벡터 변수의 경우 기울기 벡터 반환
  - 사용자 블록 내부 연산도 따라가며 미분
- 확인
  - 값 보기

## Quick Draw 데이터

`data/` 폴더에 15개 종류를 각각 10,000장씩 저장합니다.

- airplane
- apple
- bicycle
- car
- cat
- clock
- cup
- face
- fish
- flower
- guitar
- house
- star
- tree
- umbrella

각 파일은 28×28 grayscale 그림 10,000장을 연속된 `uint8` 값으로 저장한 `.bin` 파일이며 크기는 `7,840,000 bytes`입니다.

오른쪽 데이터 패널에서 사용할 종류만 선택해 메모리에 불러온 뒤, 작업공간의 데이터 블록을 통해 실제 계산 그래프에 연결할 수 있습니다.

데이터 출처: **Google Quick, Draw! Dataset**

- https://quickdraw.withgoogle.com/data
- https://github.com/googlecreativelab/quickdraw-dataset

## 의도적으로 없는 것

현재 코드에는 다음을 완성된 AI 기능으로 미리 넣지 않습니다.

- `NeuralNetwork` 클래스
- 완성된 신경망 레이어
- 가중치/편향 전용 블록
- 학습 버튼
- 완성된 역전파/경사하강법 버튼

대신 일반 변수, 벡터, 내적, 함수, 미분, 사용자 블록을 이용해 발표 과정에서 해당 수학 구조를 직접 만들어 가는 방향입니다.

## 실행

정적 HTML/CSS/JS 프로젝트입니다.

```bash
python -m http.server 8000
```

그 뒤 `http://localhost:8000`으로 접속합니다.

`index.html`을 직접 열어도 동작하도록 Quick Draw 데이터는 필요할 때 GitHub raw 경로를 사용할 수 있게 되어 있습니다.
